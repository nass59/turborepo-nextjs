import { SidebarNavItem } from "types";

interface DocsSidebarNavProps {
  items: SidebarNavItem[];
}
export const DocsSidebarNav = ({ items }: DocsSidebarNavProps) => {
  return (
    <div>
      <div>DocsSidebarNav</div>
    </div>
  );
};
