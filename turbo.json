{
  "$schema": "https://turborepo.org/schema.json",
  "globalDependencies": ["$DB_HOST"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**"]
    },
    "lint": {
      "outputs": []
    },
    "dev": {
      "cache": false
    }
  }
}
